name: sportbet
description: Ballista - A sports betting application with modern UI
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.0.0
  flutter_dotenv: ^5.1.0
  http: ^1.1.0
  image_picker: ^1.0.7
  url_launcher: ^6.3.0
  share_plus: ^10.0.0
  qr_flutter: ^4.1.0
  flutter_contacts: ^1.1.9+2
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true


